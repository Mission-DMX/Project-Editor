name: Testing

on: [ pull_request ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Test with ruff
        uses: astral-sh/ruff-action@v3
        with:
          src: "./src"


